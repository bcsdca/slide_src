/*function array_split1(arr, chunk) {

    while((arr !== undefined) && (arr.length > 0)) {

        let tempArray;
        tempArray = arr.splice(0, chunk);
        console.log(tempArray);
        console.log("The size of the original array is %d", arr.length);
    }
}

const array1 = [1, 2, 3, 4, 5, 6, 7, 8];
const chunk1 = 2;
array_split1(array1, chunk1);
*/